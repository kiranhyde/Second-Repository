name: priority_todo
description: Cross-platform priority-based to-do app with smart ingestion.
publish_to: "none"
version: 0.1.0
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  http: ^1.2.2
  intl: ^0.19.0
  uuid: ^4.5.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
